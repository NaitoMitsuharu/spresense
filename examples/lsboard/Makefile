
include $(APPDIR)/Make.defs
include $(SDKDIR)/Make.defs

PROGNAME = $(CONFIG_EXAMPLES_LSBOARD_PROGNAME)
PRIORITY = $(CONFIG_EXAMPLES_LSBOARD_PRIORITY)
STACKSIZE = $(CONFIG_EXAMPLES_LSBOARD_STACKSIZE)
MODULE = $(CONFIG_EXAMPLES_LSBOARD)

ASRCS =
CSRCS =
MAINSRC = lsboard_main.c

include $(APPDIR)/Application.mk
